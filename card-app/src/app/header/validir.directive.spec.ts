import { ValidirDirective } from './validir.directive';

describe('ValidirDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidirDirective();
    expect(directive).toBeTruthy();
  });
});
